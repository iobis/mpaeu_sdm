# Project title: MPA Europe - Species Distribution Modeling (OBIS contribution)
# Authors: Silas Principe (s.principe@unesco.org), Pieter Provoost
#
# This is a configuration file for the SDMs. It's used to configure (1) the groups
# for which to obtain the SDMs and (2) the variables to use when fitting the SDMs
# YAML files use the identation to establish list levels, so it's important to keep it consistent
# Don't use tabulation, use 2 spaces instead.
#
# File version: 2 (2024-03-18)
---
# Fill here with the conditions to get the groups.
# For example, the group photosynthesizers have to met the condition
# kingdom == 'Chromista' | kingdom == 'Plantae'
# For character, use single ''
groups:
  fishes: class == 'Elasmobranchii' | class == 'Teleostei'
  photosynthesizers: kingdom == 'Chromista' | kingdom == 'Plantae'
  seabirds: class == 'Aves'
  crustaceans: class %in% c('Copepoda', 'Malacostraca', 'Ostracoda', 'Thecostraca', 'Copepoda', 'Ichthyostraca', 'Tantulocarida', 'Thecostraca', 'Branchiopoda')
  corals: order %in% c('Scleractinia', 'Corallimorpharia', 'Antipatharia', 'Scleralcyonacea', 'Zoantharia', 'Malacalcyonacea')
  echinodermata: phylum == 'Echinodermata'
  cetaceans: order == 'Cetartiodactyla'
  mammals: class == 'Mammalia' & order != 'Cetartiodactyla'
  others: kingdom != 'Chromista' & kingdom != 'Plantae' & class != 'Aves' & class != 'Elasmobranchii' & class != 'Teleostei'
  
# Put here the list of variables that will be used for each group
# The order is variables (don't edit) > group name (should be equal to the previous section) > 'hypothesis' > list of variables
# setting an hypothesis ("name of hypothesis") is useful if you want to test multiple variables settings
# If you want to use a single hypothesis, simple leave only one hypothesis.
# Names of variable should be written on the format NAME_VARIANT(e.g. mean, min, max)
# So, for example, to get the mean temperature for the sea we would set: thetao_mean
# Example:
# variables:
#  fishes:
#    hypothesis1:
#      - variable1_mean
#      - variable2_max
#    hypothesis2:
#      - variable2_max
#      - variable3_min
variables:
  photosynthesizers:
    basevars:
      - thetao_mean
      - so_min
      - po4_mean
      - siconc_mean
      - sws_max
      - par_mean
    calcifiers:
      - thetao_mean
      - so_min
      - no3_mean
      - siconc_mean
      - sws_max
      - par_mean
      - ph_mean
    coastal:
      - thetao_mean
      - so_min
      - po4_mean
      - siconc_mean
      - sws_max
      - par_mean
      - wavefetch
  fishes:
    basevars:
      - thetao_mean
      - so_mean
      - chl_mean
      - o2_min
      - sws_max
    depthdep:
      - thetao_mean
      - so_mean
      - chl_mean
      - o2_min
      - sws_max
      - bathymetry_mean
    coastal:
      - thetao_mean
      - so_mean
      - chl_mean
      - o2_min
      - sws_max
      - wavefetch
    reef:
      - thetao_mean
      - so_mean
      - chl_mean
      - o2_min
      - sws_max
      - ph_mean
      - si_max
      - distestuary
  seabirds:
    basevars:
      - thetao_mean
      - so_mean
      - siconc_max
      - chl_mean
      - distcoast
    airtemp:
      - so_mean
      - siconc_max
      - chl_mean
      - distcoast
      - tas_mean
    totalphyto:
      - thetao_mean
      - so_mean
      - siconc_max
      - phyc_mean
      - distcoast
  cetaceans:
    basevars:
      - thetao_mean
      - sws_mean
      - mlotst_mean
      - chl_mean
      - kd_mean
    coastdep:
      - thetao_mean
      - sws_mean
      - mlotst_mean
      - chl_mean
      - kd_mean
      - distcoast
      - bathymetry_mean
    seaice:
      - sws_mean
      - mlotst_mean
      - chl_mean
      - kd_mean
      - siconc_max
      - seaicedist_mean
  mammals:
    basevars:
      - thetao_max
      - sws_mean
      - slope
      - chl_mean
      - kd_mean
      - siconc_max
      - seaicedist_mean
    airtemp:
      - tas_min
      - sws_mean
      - slope
      - chl_mean
      - kd_mean
      - siconc_max
      - seaicedist_mean
  echinodermata:
    basevars:
      - thetao_mean
      - so_mean
      - chl_mean
      - ph_mean
      - bathymetry_mean
    antartic:
      - thetao_mean
      - so_mean
      - chl_mean
      - ph_mean
      - bathymetry_mean
      - siconc_max
    coastal:
      - thetao_mean
      - so_mean
      - chl_mean
      - ph_mean
      - bathymetry_mean
      - slope
      - wavefetch
  crustaceans:
    basevars:
      - thetao_mean
      - so_mean
      - chl_mean
      - ph_mean
      - sws_mean
    antartic:
      - thetao_max
      - so_mean
      - chl_mean
      - ph_mean
      - sws_mean
      - siconc_max
    coastal:
      - thetao_mean
      - so_mean
      - chl_mean
      - ph_mean
      - sws_mean
      - wavefetch
    estuarine:
      - thetao_mean
      - so_mean
      - chl_mean
      - ph_mean
      - sws_mean
      - distestuary
  corals:
    basevars:
      - thetao_max
      - so_min
      - chl_mean
      - po4_mean
      - ph_mean
      - sws_mean
      - bathymetry_mean
    lightav:
      - thetao_max
      - so_min
      - chl_mean
      - ph_mean
      - sws_mean
      - si_mean
      - par_mean
      - bathymetry_mean
    coldwater:
      - thetao_mean
      - so_min
      - po4_mean
      - ph_mean
      - sws_mean
      - bathymetry_mean
      - slope
  others:
    basevars:
      - thetao_mean
      - thetao_range
      - so_mean
      - chl_mean
      - ph_mean
      - bathymetry_mean
    coastal:
      - thetao_mean
      - thetao_range
      - so_mean
      - chl_mean
      - ph_mean
      - bathymetry_mean
      - wavefetch
    icedep:
      - thetao_mean
      - thetao_range
      - so_mean
      - chl_mean
      - ph_mean
      - bathymetry_mean
      - siconc_max
    oxygendep:
      - thetao_mean
      - thetao_range
      - so_mean
      - chl_mean
      - ph_mean
      - bathymetry_mean
      - o2_mean

# END of configuration file